## Set-up 

1. Clone the repository with https or ssh:
2. Ensure that you have python 3.12 installed on a Windows system:
3. Create a virtual environment using 3.12:
4. Install the required dependencies:

   ```
   pip install -r requirements.txt
   ```
